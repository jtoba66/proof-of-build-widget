{
  "name": "proof-of-build-widget",
  "version": "1.0.0",
  "description": "Local MVP for proof-of-build widget with SQLite persistence",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "playwright": "^1.58.2"
  }
}
